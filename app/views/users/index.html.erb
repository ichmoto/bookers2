<%= render 'list', user: @user %>
<%= render 'books/list', book: @book %>

<h2>Users</h2>
<% @users.each do |user| %>
  <div>
    <p>image：<%= image_tag user.get_profile_image(50,50) %></p>
    <p>name：<%= user.name %></p>
    <%= link_to "Show", user_path(user.id) %>
  </div>
<% end %>